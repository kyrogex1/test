import React, { useState } from 'react';
import Header from './Header';
import BoardContainer from './BoardContainer';

const App = props => {

    return (    
        <BoardContainer/>
    );
}

export default App;